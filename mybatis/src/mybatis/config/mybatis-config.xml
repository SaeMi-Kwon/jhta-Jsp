<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
  <environments default="development">
    <environment id="development">
      <transactionManager type="JDBC"/>
      <dataSource type="POOLED">
        <property name="driver" value="oracle.jdbc.OracleDriver"/>
        <property name="url" value="jdbc:oracle:thin:@localhost:1521:xe"/>
        <property name="username" value="c##scott"/>
        <property name="password" value="tiger"/>
      </dataSource>
    </environment>
  </environments>
  <mappers>
  	<!-- 읽어올 Mapper XML 파일 포함하기 패키지명을 .으로 구분하면 안되고 디렉토리구분자(/)로 써야 함 -->
    <mapper resource="mybatis/mapper/MembersMapper.xml"/>
    
    <!--  아래처럼 여러개 파일을 포함할 수 있다.
    <mapper resource="mybatis/mapper/MembersMapper2.xml"/>
     -->
  </mappers>
</configuration>